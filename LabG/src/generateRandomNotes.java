/*import jm.JMC;
import jm.music.data.*;
import jm.util.*;
// Method to generate random notes
private static void generateRandomNotes(Phrase phrase, int noteCount) {
    for (int i = 0; i < noteCount; i++) {
        double rhythmValue = Math.random() * 2;  // Random rhythm between 0 and 2
        Note note = new Note(SNARE_DRUM, rhythmValue);  // Pitch 38 is for snare drum
        phrase.add(note);
    }
}*/